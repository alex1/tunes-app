<%= form_for(@tune, :html => { :multipart => true }) do |f| %>
  <% if @tune.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tune.errors.count, "error") %> prohibited this tune from being saved:</h2>

      <ul>
      <% @tune.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <br />
    <%= f.text_field :name %>
  </div>
  <br/>
  <%= f.fields_for :attachments do |a| %>
      <%= a.file_field :file %>
  <% end %>
   </p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
